{% extends 'base.html.twig' %}

{% block title %}Hello QuackController!{% endblock %}

{% block body %}

    <div class="container">
        <h1>Quacker</h1>

        {% for quack in quacks %}
        <div class="quackBox">
            <div class="row">
                    <div class="four columns">
                        {{ quack.id }}
                    </div>
                    <div class="four columns">
                        {{ quack.createdAt|date() }}
                    </div>
                <div class="four columns">
                    {{ quack.duckId.duckname }}
                </div>

            </div>
            <div class="quackText">
                {{ quack.content }}
            </div>
        </div>
        {% endfor %} 
    </div>
{% endblock %}
